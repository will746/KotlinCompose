<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.kotlincompose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinCompose">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--内联函数-->
        <activity android:name=".ui.home.InlineFunActivity"/>

        <!--委托代理类-->
        <activity android:name=".ui.home.DelegateActivity"/>

        <!--数据操作-->
        <activity android:name=".kotlinbasic.ConstructorHelloActivity"/>
        <activity android:name=".kotlinbasic.SetActivity"/>

    </application>

</manifest>